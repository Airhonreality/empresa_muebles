# Estado del proyecto — dashboard

Este archivo se lee al arrancar cualquier sesión. Es un dashboard corto: en qué fase está cada línea de trabajo activa, ahora mismo. El detalle cronológico completo vive en el `estado_<linea>.md` de cada línea — no se duplica acá.

**Índice de líneas:** `arnes/lineas/REGISTRO_LINEAS.md`.

---

## Línea técnica (Ola 6/7: ERP + Sitio)

**🔴 CORRECCIÓN CRÍTICA DE ENTORNO — `v3-preview` ES LA BASE DE TRABAJO REAL, NO DESCARTABLE (2026-08-20, confirmado por Javier).** Las líneas ~54/68/81/90/101/157 de este archivo (2026-08-13 a 2026-08-19) describen `v3-preview` (Neon `ep-muddy-cherry-at5j2mz7`) como un clon de prueba "descartable, nadie más la usa" — **eso ya no es cierto y posiblemente nunca lo fue con exactitud**. Hallazgos de esta sesión (verificados por lectura directa, no por documentación vieja):
- `DATABASE_URL` en `.env.local` (la que usa `npm run dev` y todo el ERP real) apunta HOY a `ep-muddy-cherry-at5j2mz7` — es decir, a `v3-preview`, no a `dev-local`.
- `dev-local` cambió de host sin actualizar este documento: antes `ep-little-dawn-atewupng` (línea 54), ahora `ep-frosty-tooth-at624umx` — drift no documentado.
- La tabla `contratos` en `v3-preview` tiene **62 registros con `valorTotal` real**, insertados en una sola ráfaga de 20 segundos el **2026-08-16 02:49 UTC** — un script automatizado que corrió en una sesión anterior y **nunca quedó commiteado ni documentado** (`scripts/migrate-core.ts`, el único script de migración en el repo, declara explícitamente en su propio comentario que NO incluye `contratos`). Los 62 valores coinciden al peso con `NOTION_db_proyectos.csv` (export histórico de Notion, en la raíz del repo) — ese fue casi seguro el origen real.
- `proyectos` en esa misma base tiene ediciones hasta **2026-08-20 07:19 UTC** (hoy) — trabajo real de ERP (kanban, cotizador, parámetros) se ha estado guardando ahí toda la semana.
- **Javier confirmó explícitamente (2026-08-20): "la preview es mi producción en este caso."**

**Consecuencia operativa:** `scripts/migrate-core.ts` tiene un `TRUNCATE ... CASCADE` sobre `proyectos`/`espacio_variantes`/`items_variante`/`productos_catalogo`/`clientes` al inicio, pensado para poder re-correrse libremente contra una base de prueba. **Ya no se puede re-correr contra `ep-muddy-cherry-at5j2mz7` bajo ninguna circunstancia** — borraría los 62 contratos, todos los proyectos editados esta semana, y cualquier fix de código que dependa de datos reales (ej. el fix de mano-de-obra de esta misma sesión). El script fue actualizado con un guard duro que lo bloquea por completo (ver comentario en el archivo) hasta que alguien lo revise a propósito. Pendiente: decidir si `v3-preview` se re-nombra formalmente a lo que realmente es (ej. tratarla como `dev-local`/producción V3), y reconciliar qué pasó con la branch `dev-local` real (¿tiene datos, o quedó vacía cuando todo el trabajo real se fue por accidente a `v3-preview`?) — no verificado en esta sesión.

**Banda F0–F9 CERRADA** (2026-08-08, checkpoint Supervisor). Todos los planes de diseño aprobados; QA documental pasó 10/10 conditions; 17 pantallas alineadas a PLANTILLA_PANTALLA; 5 gates con predicados; glosario H07 completo.

**🚀 AJUSTES PRE-LANZAMIENTO WEB PÚBLICA (2026-08-22).** Sitio web público **100% listo estructuralmente para lanzamiento inminente y cambio de DNS**. Se ejecutaron 4 ajustes vitales de pre-lanzamiento:
1. Ocultación temporal de `/conocenos` (navegación y Home) mientras los socios refinan el discurso de marca.
2. Ocultación temporal de `/bitacora` (menú principal) mientras se completa la redacción de los artículos canónicos.
3. Remodelación completa del Footer por 4 columnas temáticas con alta prioridad y canalización B2B (`/para-arquitectos`).
4. Nueva sección de engagement B2B en la Home (`¿Eres arquitecto, diseñador de interiores o constructora?`).
*Nota:* Post-DNS el trabajo continúa únicamente desbloqueando el contenido editorial restante; estructuralmente no existen bloqueantes técnicos. Verificación: `npx tsc --noEmit` 0 errores.

**F10 — Prototipo con mocks (ABIERTA).** Plan aprobado: prototipado real de las pantallas con subsistema de datos mock controlado, antes de migraciones. Feedback de uso → hallazgos → cambios de diseño/gates/schema → migraciones reales al final.

**B1 (Kanban Comercial + Cotizador) — CERRADO (2026-08-09).** 3 pasadas de auditoría completadas: POC-15 (duplicar variante, presupuesto adicional, ImagePicker), POC-16 (jerarquía espacio→variante, ojo visibleEnPropuestaPublica, punto referencial, detalles con miniatura, ImagePicker generalizado), 3ª pasada (limpieza schema `descripcionAlternativa`, activación en header, kanban compacto, responsive móvil, patrón A.10 en M-06). Verificación mecánica: `tsc --noEmit` 0, `eslint .` 0, 29/29 tests mock-store. Checklist: `arnes/lineas/ola7/tecnico/checklist_requisitos_b1_cierre.md` (30/34 cumplidos).

**B2 Lotes A/B (F3 cronograma/gates) — CERRADO (2026-08-09, POC-17).** 7 pantallas (P-06..P-12), navegación conectada. Lote C (kanban P-01 rediseño) diferido a revisión de Javier.

**Lote F5/F6/F-02/F-03/F-07 — CERRADO (2026-08-09, POC-18).** 11 pantallas más (taller/calidad/instalación/entrega/garantía/finanzas P-16..P-23, catálogo P-27, tienda F-02, portafolio F-03) + F-07 portal cliente con auth real (`iron-session`, no mock) + P-03/F-08 (solo-lectura cotizador/propuesta pública). Desde este lote los sub-agentes usan `opencode run` + modelos Zen free en vez del Agent tool de Claude (conservar rate limit). Verificación: tsc/eslint 0 errores, 78/78 tests, build 27/27 rutas. Detalle: `arnes/lineas/ola7/tecnico/registro_hallazgos_poc4.md` POC-18. **Commiteado a `dev`** (2026-08-09, junto con B1+B2, commit `130586c` — bypass explícito de un hook pre-commit legado de Agnostic Seed, roto para este worktree V3, autorizado por Javier).

**Diamante F4 (Compras) + cierre F7 — CERRADO (2026-08-10, POC-19).** 5 pantallas: P-13 (orden de compra), P-14 (recepción, E-21), P-15 (herramientas, E-45), P-24 (pedidos web, E-44), P-26 (documentación del proyecto, E-41). F4 nunca había tenido diseño ni código — se abrió el diamante completo (5 `disenio_PXX.md`) antes de tocar código, corrigiendo ese error de progreso. P-25 (histórico) resultó ser el mismo P-20 (garantía) ya construido. Decisión de alojador de documentos resuelta (R2 imágenes / Drive Desktop local sin API para SketchUp-SDK). Panel de parámetros (`/erp/finanzas/parametros`) extendido con A-01/A-02 (comisiones y umbrales de check de producción) editables en UI. Verificación: tsc/eslint 0 errores, 85/85 tests, build 30/30 rutas. Detalle: `arnes/lineas/ola7/tecnico/registro_hallazgos_poc4.md` POC-19. **Commiteado a `dev`** (2026-08-10, commit `2133a90`).

**✅ AUDITORÍA EN VIVO DE JAVIER — 7 DIAMANTES + 3 HALLAZGOS DE CÓDIGO, EJECUTADOS (2026-08-10, POC-20).** Javier auditó el prototipo en 2 rondas y compiló todo en `arnes/lineas/ola7/tecnico/backlog_auditoria_pantallas.md` antes de ejecutar. Pidió un cambio operativo: los sub-diamantes se ejecutan **sin aprobación por paso**, con mecanismos de seguridad (se detienen antes de commit, no tocan `lib/data/`/`lib/auth/`, verificación mecánica obligatoria) — decidido vía Plan mode formal. Ejecutados: D-01 (web pública, lenguaje editorial nuevo), D-02/D-03 (sidebar del ERP + limpieza de nav), D-04 (Compras, lista de ítems vía schema aprobado + "a pedido"), D-05 (Herramientas, máquina de estados corregida), D-06 (Finanzas, dashboard de KPIs), D-07 (Taller, interacción con filtros+modal), D-08a (perfil de Persona/Proveedor, vista gerente — D-08b autogestión diferida, requiere decisión de login interno). Más C-01 (bug corregido: `ordenesCompra.crear()` no generaba obligación pendiente), C-02 (foto en Equipo), C-03 (Cronograma en el nav). Incidente de infraestructura (aislamiento por worktree incompatible con este repo, detectado y contenido sin daño) documentado en el hallazgo. Verificación: tsc/eslint 0 errores, 92/92 tests, build 40/40 rutas. Detalle: `arnes/lineas/ola7/tecnico/registro_hallazgos_poc4.md` POC-20. **Pendiente de commit** — Javier revisa en vivo primero. **DP-01/DP-02/DP-03 resueltas o diferidas** (ver `TAREAS_DIFERIDAS.md`).

**✅ RONDA 4 — CICLO COMPLETO DE 10 DIAMANTES (D-01 a D-10), EJECUTADO (2026-08-10, POC-20 ronda 4).** Javier pidió subir el rigor: cada diamante del backlog pasa por auditoría profunda (bruta→autocrítica→refinamiento) → auditoría axiomática (Suh, FR-DP) → auditoría de ergonomía cognitiva (checklist nuevo, primera versión) → requisitos de código → ejecución — aplicado tanto a D-09 (Cotizador) y D-10 (Home real), que no tenían diamante todavía, como a D-01→D-08a, ya dados por "ejecutados" con una sola pasada. La rigor pagó: se encontraron y corrigieron hallazgos reales que la pasada anterior no había detectado — fuga de PII de clientes (dirección exacta) en el portafolio público, indexable en JSON-LD (D-01); `<button>` anidado en `<a>` en la navegación principal del ERP y del sitio público, 11 archivos (D-02/D-03); ítems "eliminados" del Cotizador que seguían cobrándose en la propuesta pública del cliente (D-09c); recepción de material que nunca marcaba ítems como recibidos (D-04); una vía silenciosa para crear OC sin proveedor real (D-05); conteos de Instalación/Garantía en Taller que no implementaban lo que su propio diseño aprobado pedía (D-07); más hallazgos menores de consistencia (etiquetas crudas, formato de moneda) en D-06/D-08a. D-08b (login interno): **diseñado, no construido** por decisión de Javier — `diseno_perfil_persona_proveedor_login_interno.md` reusa el mecanismo de sesión ya aprobado del portal cliente en vez de inventar uno nuevo. **DP-01/DP-02/DP-03 resueltas o diferidas** (ver `TAREAS_DIFERIDAS.md`). Autocertificación mecánica por diamante (evidencia de comando), commit sigue siendo de Javier. Verificación: tsc/eslint 0 errores, 93/93 tests, build 42/42 rutas. Detalle completo: `arnes/lineas/ola7/tecnico/registro_hallazgos_poc4.md` POC-20 (ronda 4) y `backlog_auditoria_pantallas.md` (fila por fila). **Nada commiteado** — a la espera de la revisión en vivo de Javier sobre todo el batch (D-01 a D-10).

**✅ BATCH DE AUDITORÍA DE JAVIER + BARRIDO COMPLETO DE CUMPLIMIENTO (2026-08-10, D-11 a D-17).** Javier trajo 4 hallazgos puntuales sobre Cotizador/Contrato/Propuesta pública, más el pedido de verificar si otras pantallas se codificaron sin cotejar contra su propio `disenio_PXX.md`. Dos pasadas de barrido (Explore agents comparando diseño↔código, mismo método) cubrieron las 22 pantallas que nunca habían tenido este nivel de re-auditoría: 15 en la primera pasada (11 con hallazgos reales — fuga de datos en Tienda Web, dinero desincronizado en Caja, un comentario que mentía sobre un filtro de privacidad en Portal Cliente, entre otros) y 7 más del Bloque 2 cronograma/gates en la segunda (6 de 7 con hallazgos, uno **crítico**: el botón "Confirmar check" de producción no hacía nada en su camino normal — comparaba contra un campo que empieza en `null`). También se auditó el árbol módulo/espacio (violación de la premisa central del diseño: gates evaluados por proyecto completo en vez de por nodo — **diferido a propósito, es demasiado grande para un fix ciego**) y la Home (`F-01`, SEO/JSON-LD desactualizado + grilla de espacios sin links). La mayoría de los hallazgos puntuales se corrigieron en el momento con sub-agentes Haiku autónomos (mismo modo operativo que POC-20), verificados mecánicamente uno por uno. Quedaron pendientes, sin ejecutar todavía: 2 lotes chicos que tocan `lib/data/` (designar `verificador_id` en P-12, agregar `padreLinaje` a módulo/espacio + los métodos `porProyecto()` que le faltan a Caja/Recepción), y la pieza grande diferida de módulo/espacio. Todo registrado fila por fila en `backlog_auditoria_pantallas.md` (D-11 a D-17). **Nada de esto está commiteado** — se suma a la espera de la revisión en vivo de Javier, junto con el batch D-01→D-10 anterior.

**✅ CORTE DE BUCLE DE ARNÉS + CIERRE DE COMPILACIÓN (2026-08-12, sesión agente).** Se identificó que el arnés reabría fases porque tareas diferidas (DP-01/02/03, D-08b, pendientes de demanda D1/t-110/t-111) se contaban como bloqueantes de migración/preview. Se creó `arnes/TAREAS_DIFERIDAS.md` como fuente única de verdad de tareas postergadas a propósito, explícitamente **no bloqueantes** para migración ni preview — corta el bucle. Se confirmó que los 2 lotes de `lib/data/` (verificador_id en P-12, padreLinaje en módulo/espacio, métodos porProyecto de Caja/Recepción) **ya estaban resueltos en el working tree**. Se corrigieron los errores de `tsc` que impedían build limpio: `SHOP_CATEGORIAS` (enum usado como valor — import/export corregidos en `fixtures.ts`/`mock-store.ts`/`index.ts`/`mock-store.test.ts`), `testimonios` añadido a la interfaz `DataStore` y a `drizzle-impl.ts`, typo `producto`→`p` en `colecciones/page.tsx`, y 2 typings de PDF (`Response(new Uint8Array(pdfBuffer))` y `basePdf: null as never`). Resultado: **`tsc --noEmit` exit 0**. El prerequisito de datos para F10 queda satisfecho. **Nada commiteado** — sigue en la cola de revisión en vivo de Javier, junto con D-01→D-17.

**✅ CORRECCIÓN F-03 — DESACOPLAMIENTO DE PORTAFOLIO DE ESPACIOVARIANTE (2026-08-12, sesión agente posterior al CORTE).** Auditoría de un flujo de portafolio reveló un **acoplamiento indebido**: `app/(publico)/portafolio/[slug]/page.tsx` consumía `store.espacios.porProyecto()` y `EspacioVariante.fotosEspacio` para alimentar la galería pública del portafolio — mezclando la capa de cotización/ERP con la vitrina de marketing público (riesgo de exponer fotos técnicas/planos y romper la separación de responsabilidades). **Decisión ARCH-012:** el portafolio público se desacopla de `EspacioVariante`. Cambios ejecutados:
- `lib/data/contracts.ts` + `fixtures.ts` + `mock-store.ts`: nuevo campo `Portafolio.galeriaPortafolioUrl: string[]` (máx. 10 imágenes, orden = orden de galería); `actualizar()` lo soporta.
- `components/veta/image-picker.tsx`: extendido con `uploadToR2` + `r2Prefix` → sube archivos automáticamente a Cloudflare R2 (`lib/r2/upload.ts`, usa `@aws-sdk/client-s3`, credenciales `CF_R2_*` ya en Vercel). Fallback a previsualización local si falla.
- `app/erp/proyectos/[proyectoId]/portafolio/page.tsx`: formulario usa `ImagePicker uploadToR2` para `galeriaPortafolioUrl`; se reemplazaron `Input/Textarea/Select` inexistentes por elementos nativos (errores de import previos resueltos).
- `app/(publico)/portafolio/[slug]/page.tsx`: hero = `galeriaPortafolioUrl[0]` (o `imagenPortafolioUrl`); filmstrip + overlay restaurados sobre `galeriaPortafolioUrl`; eliminado todo consumo de `store.espacios`.
- **Ruido de documentación legacy corregido:** `disenio_F03_portafolio_proyectos.md` (mapeos de componentes §8.4, RG-2, comportamiento tabla, integridad §8.6) ya NO cita `EspacioVariante.fotosEspacio`; `REGISTRO_DE_ENTIDADES.md` §10 anota `galeria_portafolio_url` y el desacoplamiento; `disenio_F08_propuesta_publica.md` lleva nota de descruce (F-08 SÍ usa `EspacioVariante.fotosEspacio` legítimamente; F-03 NO).
- **Verificación:** `tsc --noEmit` sobre los archivos tocados no reporta errores nuevos. **⚠️ DISCREPANCIA A ESCALAR:** el `tsc --noEmit` global AÚN reporta errores preexistentes (`colecciones/page.tsx` `producto`, `SHOP_CATEGORIAS` en `fixtures.ts`/`mock-store.ts`, `testimonios` en `DataStore`, 2 typings de PDF en `cotizacion.ts`) que la entrada "CORTE DE BUCLE" (línea 29) afirma como corregidos con exit 0 — esos fixes no están presentes en este working tree. No se tocaron en esta sesión (fuera de alcance); requieren validación del Supervisor antes de confiar en "tsc exit 0".

**✅ RESOLUCIÓN DE DISCREPANCIA tsc + BLOQUEO VERCEL + OPCIÓN A (2026-08-12, sesión agente).** (a) **Discrepancia de línea 37 resuelta por verificación directa:** se re-ejecutaron los fixes de compilación (`SHOP_CATEGORIAS` valor no tipo, `testimonios` en `DataStore`/`drizzle-impl.ts`, typo `colecciones`, 2 typings PDF, hooks condicionales en `portafolio/[slug]/page.tsx`) y se verificó empíricamente: **`tsc --noEmit` exit 0** y **`eslint .` 0 errores** (23 warnings, no bloquean). Los fixes SÍ están presentes en el working tree; la escalación de la línea 37 es stale. (b) **Bloqueo Vercel descubierto:** `lib/data/store.ts` prohíbe `DATA_IMPL=mock` si `VERCEL=1`, y `createDrizzleStore()` es un stub de 311 líneas que lanza "no implementado". Conclusión: **hoy NO existe ninguna config que corra en Vercel** (ni mock ni drizzle). El preview con datos reales requiere implementar el adaptador Drizzle (F10-E) — **prerequisito ausente en el ledger t-113..117 del plan_f10_migracion.md**. (c) **Opción A implementada:** escape hatch `ALLOW_MOCK_PREVIEW=1` (solo si `VERCEL=1` Y la var está seteada) que permite mock en el entorno Preview de Vercel para validar pipeline/UI sin datos reales; **nunca setear en Production**. (d) **Fase 0 scaffolding creado** en `arnes/lineas/ola7/migracion/` (`PROCEDIMIENTO_FASE0.md`, `mapeo_campos.md` esqueleto, `riesgos_migracion.md` con riesgos conocidos + bloqueos Vercel). Fase 0 **bloqueada a la espera de la connection string de Neon `main`** (el `.env.local` solo tiene `DATABASE_URL`=dev-local; no hay `DATABASE_URL_LEGACY`).

**✅ REORDEN DE PLAN F10 — BUILD-FIRST / CLONE-LAST (2026-08-13).** El Supervisor corrigió el orden del plan F10: el orden original (diagnóstico → migrar → preview) estaba invertido. Nuevo orden validado:
1. **PRIMERO** construir y probar el schema V3 con Drizzle de forma elegante (28 tablas tipadas, migraciones `drizzle/v3/`, adapter `createDrizzleStore()` con write-through, `tsc`/`eslint`/`build`/tests) — **sin datos legacy**.
2. **LUEGO** desplegar preview de Vercel con el schema limpio y validar infraestructura.
3. **SOLO AL FINAL** clonar legacy → V3 campo-a-campo (scripts/migrate-core.ts) usando `mapeo_campos.md` (sección LIVE PRODUCTION MAPPING).
- **Diagnóstico COMPLETADO (t-125):** se consultó producción LIVE (read-only) y se documentó en `mapeo_campos.md` la sección LIVE PRODUCTION MAPPING con conteos reales — el snapshot congelado `legacy-agnostic-backup` era OBSOLETO (clientes=33 no 15; proyectos=49; productos_catalogo=278; espacio_variantes=124; items_variante=626; contratos=7; cotizaciones=18; propuestas_publicas=23). Hallazgos: V3 NO tiene tabla `cotizaciones` (→ `proyectos` estado `enviada`); `portfolio_publico` NO existe en live (→ `propuestas_publicas`→`portfolioPublico`). Legacy usa modelo Agnostic Seed = UNA tabla `agnostic_records` con `data` JSONB por namespace (no tablas relacionales).
- `plan_f10_migracion.md` reescrito con el orden corregido (Fase 0 build/test → Fase 1 preview → Fase 2 clone → Fase 3 verify). Tarea `t-126` registra la reordenación.

**✅ B-1 RESUELTO (2026-08-13, checkpoint Supervisor) — persistencia de escrituras + reactividad total.** El Supervisor exigió reactividad total y no negociable para todo el equipo: cero recarga manual, cero pasos intermedios. Un análisis previo del problema subestimaba el alcance real (23 archivos/44 escrituras "concentradas en cotizador" — la cifra real verificada contra el código es **43 archivos/305 lecturas y 96 escrituras en 25 archivos**, repartidas por todo el ERP) y no detectaba que los 45 consumidores de `useDataStore()` son **todos `'use client'`** — el driver `postgres` no puede correr en el navegador, así que ningún adapter puede hablar con Neon directo desde el cliente, con o sin `await`. Decisión final (detalle en `plan_f10_migracion.md` §3.1b/§3.1d, tareas t-130/t-131 nuevas):
- Escrituras (~96 call sites) → capa de **Server Actions** que confirman en Neon antes de retornar (sin fire-and-forget, sin pérdida de datos).
- Hidratación inicial → **un solo punto** (layout raíz Server Component → Provider cliente), sin loading flicker en las 43 pantallas.
- Lecturas (209 call sites) → **sin cambios**, siguen síncronas contra la caché en memoria.
- Reactividad multi-usuario → **polling corto (2-4s) con verificación liviana** antes de traer el snapshot completo, reusando el mecanismo M-07 (`subscribe()`/`getVersion()`) ya existente. Sin websockets/pub-sub de terceros (evaluado y descartado: infraestructura nueva, checkpoint propio, ganancia marginal frente al requisito de ≤4s).

**✅ B-3 RESUELTO + SCHEMA V3 CONSTRUIDO Y MIGRADO (2026-08-13, misma sesión).** El Supervisor confirmó por API key que el único proyecto Neon válido es `veta_dorada_db` (cuenta `vetadeoro.co@gmail.com`, us-east-1, org `veta_dorada`) — `empresa_muebles_database` (sa-east-1) era un proyecto equivocado de otra cuenta, purgado de `.env.local` y de la documentación. Branches nuevas, copy-on-write de `production` (`ep-round-queen-at3nzf87`, la base real): `dev-local` (`ep-little-dawn-atewupng`) para trabajo diario, `v3-preview` (`ep-muddy-cherry-at5j2mz7`) para Fase 1. Además se encontró que `lib/db/schema.ts` (28 tablas) estaba muy por detrás de `lib/data/contracts.ts` (55 dominios) — le faltaban ~30 tablas completas (todo F3 cronograma/gates, F4 compras, F5 taller/calidad/instalación/garantía, F-02 tienda, F-03 portafolio nuevo con `galeriaPortafolioUrl`/ARCH-012, F-15 bitácora, testimonios) y tenía columnas desalineadas en tablas existentes (`proyectos` sin `verificadorId`/`comercialVendedorId`/`fechaEntradaDesarrollo`; `contratos.condicionesDesmonte`→`especificacionesDesmonte`; `espacio_variantes.visiblePdf`→`visibleEnPropuestaPublica`; `proveedores` sin columnas de despacho/pago; `estado_proyecto` enum con 9 de 19 valores reales). Se escribió el schema completo (58 tablas, derivadas 1:1 de `contracts.ts`), se generaron migraciones frescas en `drizzle/v3/` (`0000_mature_rumiko_fujikawa.sql` + `0001_common_mystique.sql`, fix de 5 nombres de constraint truncados por el límite de 63 caracteres de Postgres) y se aplicaron contra `dev-local` sin errores. Verificado: `tsc --noEmit` limpio en cada paso, `drizzle-kit generate` reporta **"No schema changes, nothing to migrate"** (sin drift). CA-0/CA-2/V-1 del plan quedan cumplidos. Detalle: `plan_f10_migracion.md` §3.5.

**✅ FASE 0 COMPLETA — MOTOR DE DATOS REAL FUNCIONANDO CONTRA NEON (2026-08-14, misma sesión).** t-119/t-130/t-131 implementados y verificados end-to-end:
- **Schema**: `lib/db/schema.ts` completado a 58+ tablas (de las 28 originales), 6 tablas legacy reconciliadas campo a campo contra `contracts.ts` (`personas`, `productos_catalogo`, `pedidos_web`, `cuentas_financieras`, `movimientos_financieros`, `obligaciones_pendientes`), migraciones frescas en `drizzle/v3/` aplicadas a `dev-local` sin drift.
- **Contrato async**: los 98 métodos de escritura de `DataStore` (`contracts.ts`) convertidos a `Promise<T>`; `mock-store.ts` y sus 73 tests migrados a `async/await` (73/73 pasan); los ~98 sitios de escritura en `app/` (25 archivos) recableados con `await` real, incluidos los ~75 que TypeScript no marcaba por ser fire-and-forget.
- **Server Actions reales**: ~99 funciones `'use server'` en `lib/data/actions/{core,f3,f4,f5,f6,f7-tienda,portafolio}.ts`, cada una con Drizzle real + transacciones donde aplica, reusando los validadores puros de `lib/modules/f3/gates.ts` y `lib/modules/f4f5f6/gates.ts` (mismos predicados que ya pasaban los 73 tests del mock — sin reimplementar reglas de negocio).
- **Store del navegador**: `lib/data/drizzle-impl.ts` reescrito como `createDrizzleStore(snapshot)` — caché en memoria + lecturas síncronas (idénticas a mock-store) + escrituras que llaman al Server Action, esperan confirmación real, y solo entonces actualizan caché + notifican (sin fire-and-forget, sin pérdida de datos).
- **Reactividad sin recarga**: `DataStoreProvider` (Context, `lib/data/DataStoreProvider.tsx`) recibe el snapshot inicial hidratado server-side por `app/layout.tsx` (sin loading flicker) y hace polling cada 3s contra `fetchVersionTokenAction()` (query barata); si detecta cambios, trae el snapshot completo y lo aplica. `useDataStore()` (`index.ts`) ahora lee del Context en vez de un singleton.
- **`lib/auth/session.ts`**: `login()` ajustado para consultar Drizzle directo cuando `DATA_IMPL=drizzle` (no puede usar `getDataStore()`, que ahora lanza a propósito en ese modo).
- **Verificación real, no solo tipos**: `tsc --noEmit` limpio, `eslint .` 0 errores, `next build` exitoso **con `DATA_IMPL=mock` Y con `DATA_IMPL=drizzle` contra `dev-local` real** (las 47 rutas prerenderizan con Postgres real). Prueba E2E manual (creada y borrada en la misma sesión): crear cliente → crear proyecto → intentar transición de estado (rechazada correctamente por falta de parámetro, validando que la regla de negocio corre de verdad) → confirmar ambos registros en el snapshot leído de vuelta.
- **Alcance no cubierto todavía (documentado, no bloqueante para el flujo core)**: unas pocas escrituras que crean un registro secundario en la misma transacción del servidor (ej. `contratos.crear` también inserta hitos; `espacios.duplicar(vacio:false)` también clona items/artefactos) actualizan el registro principal en la caché del cliente al instante, pero el secundario llega recién con el siguiente ciclo de poll (≤4s) — degradación aceptada explícita, no un bug.

**✅ REACTIVIDAD MULTI-USUARIO REVISADA E IMPLEMENTADA (t-132, 2026-08-14, misma sesión).** El polling de 3s de `t-131` (sin pausa por visibilidad) agotaba la cuota de Vercel Hobby (100.000/mes) con el equipo real (~5 usuarios) en ~1 día. Llegó una recomendación externa para resolverlo con `BroadcastChannel` + foco + poll pasivo 30s — auditada y **rechazada**: `BroadcastChannel` no cruza máquinas, no resuelve el caso cross-usuario, y su respaldo degradaba en silencio el SLA de ≤4s ya aprobado. Archivada como MAL PLAN en `arnes/lineas/ola7/archivo/MAL_PLAN_reactividad_broadcastchannel_DEPRECADO.md`. Implementado en su lugar: **long-polling acotado (20s) + `LISTEN`/`NOTIFY` de Postgres** vía un trigger genérico en las 64 tablas de negocio (`drizzle/v3/0004_veta_notify_trigger.sql`, aplicado a `dev-local`, 64/64 confirmados) + `lib/data/actions/longpoll.ts` (`.listen()` de `postgres.js`, sin dependencias nuevas) + `DataStoreProvider.tsx` reescrito (pestaña líder vía `navigator.locks`, `BroadcastChannel('veta_erp_reactividad')` para fan-out entre pestañas propias, pausa real a los 12 min de inactividad). Verificado: `tsc`/`eslint`/`next build` (mock y drizzle) limpios, y 2 smoke tests E2E contra `dev-local` real (escritura real → resuelve en 2.4s con `changed=true`; sin escritura → timeout limpio en 20.1s). Hallazgo en el camino: `fetchVersionTokenAction()` (de `t-131`) solo cubría 12/64 tablas — corregido para que `changed` salga siempre de un `NOTIFY` real, no de comparar version-strings; la fórmula en sí queda como mejora futura no bloqueante (requeriría agregar `updated_at` a 43 tablas que no lo tienen, cambio de schema con checkpoint propio). Detalle completo en `arnes/lineas/ola7/tecnico/m07b_reactividad_multiusuario.md`. **Pendiente, no verificable en este entorno headless:** V-5b con dos pestañas/usuarios reales de navegador simultáneas, incluido el caso de cerrar la pestaña líder — requiere sesión manual del Supervisor con `npm run dev` y `DATA_IMPL=drizzle`.

**✅ CLONE DE PRUEBA A `v3-preview` EJECUTADO (2026-08-14, misma sesión).** Con las 5 decisiones de `B-2` ya tomadas, el Supervisor pidió una corrida de prueba directa (leyendo `production` real, no la copia de `dev-local`) para revisar manualmente antes de aprobar nada como definitivo — decidido explícitamente como reversible (`v3-preview` es descartable, nadie más la usa). `scripts/migrate-core.ts` (nuevo, idempotente) migró: `clientes` 33/33, `productos_catalogo` 278/278 (SKU autogenerado para todos), `proyectos` 51/51, `espacio_variantes` 137/139 (2 descartados sin proyecto padre), `items_variante` 531/630 (99 descartados sin espacio padre). 0 FKs rotas verificadas en destino. **Hallazgo encontrado al escribir el script, no visto en la primera pasada del dry-run:** `cotizaciones` no es un namespace de entidades separadas — sus 18 registros comparten el mismo `id` que 18 de los 51 `proyectos` (mismo negocio, dos vistas). El mapeo original (`cotizaciones → proyectos` nuevo con `estado='enviada'`) habría duplicado esos 18 proyectos reales — se corrigió para migrar solo desde `proyectos`. Detalle completo en `validacion_valores.md`. **Alcance de esta prueba:** solo `clientes`+`proyectos`+`espacios`+`items`+`catalogo`, por decisión explícita del Supervisor — `contratos`/`hitos_pago`/`propuestas_publicas`/`portafolio` quedan fuera (los últimos dos se regeneran solos, contratos no se pidió). **No se desplegó a Vercel Preview** (eso es Fase 1 propiamente dicha, todavía no ejecutada) — la revisión debe hacerse vía `drizzle-kit studio` o corriendo la app local apuntada a `v3-preview`. El merge a `main` sigue completamente fuera de este alcance.

**✅ FOOTER EDITORIAL PREMIUM + WHATSAPP FLOTANTE — DP-04 resuelto parcialmente (2026-08-15, sesión agente).** Revisión encontró que `app-shell.tsx` implementaba un footer de una línea y un nav de 4 ítems, muy por debajo de lo que `contenido_F00_shell.md` (copy aprobado 2026-08-09) especifica. Ejecutado: footer de 4 columnas (marca+eslogan, NAP vía `MetaItem`, enlaces a rutas reales, legal+NIT+copyright) y `components/veta/whatsapp-float.tsx` (botón flotante transversal, montado en `AppShell`), copy literal del documento aprobado, sin fabricar texto nuevo. Diseño formal en `arnes/lineas/ola7/pantallas/disenio_F00_shell.md` (cierra el gap DP-04 de `TAREAS_DIFERIDAS.md`). **Deliberadamente fuera de esta pasada:** CTA "Agenda tu Asesoría" + modal transversal DC-3 (necesitan un dominio `leads` nuevo en `lib/data/`, zona de riesgo alto — requiere su propio plan) y 4 ítems de nav (Espacios/Cómo Trabajamos/Conócenos/Para Arquitectos) porque sus páginas (F-10/F-11/F-18/F-19) no existen todavía — agregarlos habría creado enlaces rotos. Verificado: `tsc --noEmit` limpio, `eslint .` 0 errores (22 warnings preexistentes, ninguno en los archivos tocados), `next build` 32/32 rutas. **Nada commiteado.**

**✅ LOGIN INTERNO DEL ERP IMPLEMENTADO — D-08b CONSTRUIDO (2026-08-15, sesión agente, checkpoint Javier vía plan mode).** Hallazgo que disparó la tarea: `/erp/**` no tenía ningún gate (no existía `middleware.ts`), y `usuarioActual()` en modo Drizzle hacía `SELECT * FROM usuarios LIMIT 1` — cualquiera con la URL del preview tenía acceso de facto con rol admin. D-08b (`diseno_perfil_persona_proveedor_login_interno.md`) ya había dejado diseñado (no construido) reusar el mecanismo de `iron-session` del portal cliente — este es el plan de ejecución que faltaba, aprobado por Javier vía plan mode antes de tocar código (mutación de `lib/auth/` + schema, riesgo alto). Construido:
- **Schema:** `usuarios.passwordHash` ahora nullable + `inviteToken`/`inviteTokenExpiraEn` nuevos (migración `drizzle/v3/0005_open_crystal.sql`, aditiva/no-destructiva, `generate` confirma sin drift tras aplicar).
- **Sesión de empleado separada** (`lib/auth/session.ts`): cookie `veta_erp_session` (distinta de `veta_cliente_session`), `loginEmpleado`/`logoutEmpleado`/`requireSesionEmpleado`/`crearInvitacionEmpleado`/`activarInvitacion`/`listarEstadoCuentasEmpleado`, hash con `crypto.scrypt` nativo (sin dependencia nueva), bifurcación mock/drizzle igual que `login()` ya existente.
- **`middleware.ts` nuevo** (no existía) — gatea `/erp/:path*` excepto `/erp/login*`, desella la cookie en Edge runtime sin tocar DB.
- **Rutas nuevas:** `/erp/login`, `/erp/login/activar?token=...`.
- **`usuarioActual()` deja de mentir:** `fetchSnapshotAction` (`lib/data/actions/hydrate.ts`) ahora puebla `usuario` desde la sesión real (sin query extra a DB), corrigiendo de paso 3 sitios que ya atribuían acciones a `usuario.id` (`compras`, `calidad`, `desarrollo`) — antes siempre el mismo placeholder.
- **Equipo (`app/erp/equipo/page.tsx`):** "Nuevo empleado" ahora pide email+rol y genera el enlace de autoregistro en el mismo paso (caja copiable, `components/veta/copy-field.tsx`); cada persona sin cuenta activa tiene botón "Generar acceso"/"Reenviar enlace"; estado de cuenta (sin cuenta/pendiente/activa) visible por persona vía Server Action saneada (nunca expone `passwordHash` ni el token salvo al crearlo).
- **Bootstrap:** `scripts/seed-admin-empleado.ts` (mismo guard de allowlist de host que `seed-dev.ts`) — resuelve el problema de arranque (nadie puede autoinvitarse por UI si `/erp/equipo` ya está gateado). Ejecutado una vez: se creó la Persona real de Javier García (`personas`, rol admin en `personasRoles`) y su cuenta admin activa.
- **Verificado:** `tsc --noEmit` y `eslint .` limpios (0 errores, mismos warnings preexistentes), `next build` con `DATA_IMPL=drizzle` 51/51 rutas, `drizzle-kit generate` sin drift tras migrar. E2E real contra la DB conectada hoy vía `.env.local` (ver discrepancia abajo): middleware bloquea `/erp/catalogo` sin cookie (307→`/erp/login`) y lo permite con sesión válida (200); login real con la cuenta sembrada; ciclo completo de invitación→activación con una persona de prueba (creada y borrada en la misma sesión) confirmando rol/nombre reales en el sidebar y reintento de token ya usado rechazado (`token_invalido`); logout limpia la cookie y vuelve a bloquear.
- **⚠️ Discrepancia de branch de Neon encontrada (sin resolver, no bloqueante para esta tarea):** `arnes/estado.md` (línea ~54) documenta `dev-local = ep-little-dawn-atewupng` y `v3-preview = ep-muddy-cherry-at5j2mz7`, pero el `DATABASE_URL` que hoy resuelve realmente `.env.local` apunta a `ep-muddy-cherry-at5j2mz7` (es decir, **v3-preview**, no `dev-local` como asume `drizzle.config.ts` en su comentario). Además `scripts/seed-dev.ts` tiene un allowlist con un tercer host (`ep-silent-field-ac8slpbc-pooler.sa-east-1...`) que no coincide con ninguno de los dos — drift preexistente, no introducido en esta sesión. La migración 0005 y el E2E de esta tarea corrieron contra el host real verificado (`ep-muddy-cherry-at5j2mz7`), no contra el nombre documentado. Pendiente: que el Supervisor reconcilie qué branch de Neon es cuál y actualice `.env.local`/`drizzle.config.ts`/`seed-dev.ts` en consecuencia.

**✅ DATOS LEGALES/RRHH DEL EMPLEADO — dirección + 2 referencias personales (2026-08-15, sesión agente, continuación directa de la tarea de login interno).** Javier pidió agregar los datos que faltaban en el perfil de Persona: dirección, y 2 referencias personales (nombre + relación/parentesco + teléfono cada una — confirmado con Javier vía pregunta directa, no asumido). Documento (cédula), teléfono y correo ya existían. Construido:
- **Schema:** `personas` +7 columnas nullable (`direccion`, `referencia_1_nombre/relacion/telefono`, `referencia_2_nombre/relacion/telefono`) — migración `drizzle/v3/0007_curvy_trish_tilby.sql`, aditiva/no-destructiva.
- **`lib/data/contracts.ts`** (`Persona`, `DataStore.personas.crear/actualizar`), **`lib/data/actions/f3.ts`** (`crearPersonaAction`/`actualizarPersonaAction`), **`lib/data/mock-store.ts`** + **`fixtures.ts`** (6 personas existentes con los campos nuevos en `null`) actualizados en paralelo mock/drizzle, mismo patrón que el resto de `Persona`.
- **UI:** `app/erp/equipo/[personaId]/page.tsx` — "Dirección" sumado a la tarjeta de datos de contacto; tarjeta nueva "Referencias personales" (2 bloques de nombre/relación/teléfono en modo edición; vista de lectura oculta bloques vacíos). Deliberadamente NO se agregó al form compacto de creación en `/erp/equipo` (mismo criterio que documento/teléfono ya usaban: se cargan después, en el perfil).
- **⚠️ Hallazgo de infraestructura durante la migración (no mío, de otra sesión concurrente sobre este mismo worktree):** `drizzle/v3/0006_add_galeria_catalogo.sql` (columna `galeria_imagenes_url` en `productos_catalogo`, t-139) existía en disco con su entrada en `_journal.json` pero **sin su `0006_snapshot.json`** y **sin haber sido aplicada nunca a la DB** — un `drizzle-kit generate` mío iba a reincluir esa misma columna en mi migración 0007 (choque). Se corrigió quitando la línea duplicada de mi 0007 antes de aplicar; `migrate` corrió 0006+0007 limpio, sin drift después. **Aparte, y sin relación con lo anterior:** hoy `npx tsc --noEmit` global reporta 15 errores preexistentes en `app/erp/catalogo/page.tsx` / `lib/data/actions/core.ts` (nuevo, sin trackear) / `lib/data/fixtures.ts` (mismo t-139, `ProductoCatalogo.galeriaImagenesUrl` a medio propagar) — trabajo en curso de otra sesión, no tocado ni arreglado acá (fuera de alcance, no relacionado con Persona). Confirmado que ninguno de esos 15 errores toca `Persona`/`personas`; `tsc`+`eslint` sobre los archivos de esta tarea están limpios y probados contra la DB real (crear→actualizar→leer→borrar).

**🔴 BLOQUEOS PENDIENTES — requieren decisión del Supervisor antes de ejecutar:**
- **B-2 (t-129) — ✅ RESUELTO 2026-08-14.** Dry-run exhaustivo (100% de los registros) contra `agnostic_records`, con el hallazgo de huérfanos re-verificado contra `production` en vivo (misma cifra: 97). 5/5 decisiones de remediation tomadas por el Supervisor: SKU se regenera automáticamente para los 278 productos; 2 `espacio_variantes` + 97 `items_variante` huérfanos (19 espacios fantasma borrados sin cascada en el legacy) se descartan, 1 proyecto con nombre vacío pero con hijo real se conserva; `estado='visita_tecnica'` remapea a `negociacion`; `estado` NULL (13 proyectos + 13 cotizaciones) default a `borrador` (transición manual posterior); `propuestas_publicas`/portafolio web público **no se migran**, se regeneran solos — el clone real de Fase 2 es `proyectos`+`espacios`+`items`+`clientes`+`catalogo`. Detalle completo en `arnes/lineas/ola7/migracion/validacion_valores.md`. De paso: `DATABASE_URL_LEGACY` (producción, solo lectura) ya está en `.env.local`, y se confirmó que `dev-local` tiene drift real frente a producción (+1 proyecto/+1 espacio/+4 items/+1 propuesta) — la Fase 2 debe leer de producción en vivo, no de la copia congelada.
- **B-4 (t-128, parcial):** aprobar plan de rollback/corte. El proyecto Neon ya no es ambiguo (B-3 resuelto). Pendiente menor no bloqueante: confirmar a qué host apunta hoy el `DATABASE_URL` real de Vercel (`empresa-muebles-vl37`, compartido Preview+Production) — no se pudo leer el valor por bloqueo de seguridad del sandbox al intentar volcarlo a disco.

**Próxima acción permitida (ORDEN CORREGIDO + bloqueos):**
1. **Fase 0 (Construir schema V3 + tests, t-119 + t-130 + t-131): ✅ COMPLETA (2026-08-14).** Schema (58+ tablas, 6 reconciliadas), contrato async (98 métodos), 99 Server Actions reales, `createDrizzleStore()` (lecturas + delegación de escrituras), `DataStoreProvider` (hidratación SSR + polling 3s), `lib/auth/session.ts` ajustado. Verificado: `tsc`/`eslint`/`next build` limpios en ambos `DATA_IMPL` (mock y drizzle contra `dev-local` real), E2E manual de escritura+lectura con regla de negocio validada de verdad. Pendiente menor, no bloqueante: probar V-5b (dos pestañas de navegador reales) con `npm run dev` — no ejecutable en este entorno headless, requiere sesión manual o Playwright.
2. **Fase 0.5 (Validación de valores, t-129): ✅ EJECUTADA (2026-08-14), 🔴 NO APROBADA.** 7 hallazgos críticos reales (ver arriba y `validacion_valores.md`) — quedan 5 decisiones de negocio del Supervisor antes de que B-2 se pueda dar por resuelto.
3. **Fase 1 (Preview Vercel vacío):** aplicar migraciones `drizzle/v3/` a `v3-preview` (mismo schema que `dev-local`, aún no aplicado ahí), apuntar el env de Preview de Vercel a esa branch. Push `dev` → preview; `DATA_IMPL=drizzle`; validar infraestructura. 🔴 **B-4 (t-128): aprobar plan de rollback ANTES de desplegar.**
4. **Fase 2 (Clone legacy, t-115/116):** backup CA-7, luego `scripts/migrate-core.ts` lee `main` (read-only) y escribe `v3-preview` campo-a-campo usando `mapeo_campos.md` LIVE. Requiere B-2 y mapeo aprobado (CA-1).
5. **Fase 3 (Verificación, t-117):** conteos + V-5b (escritura de un usuario visible para otro en ≤4s sin recargar).
6. **Corte (t-118):** si Javier aprueba, merge `dev` → `main`.

> Nota: el clone NO requiere `DATABASE_URL_LEGACY` — corrección 2026-08-14, `dev-local` ya trae `agnostic_records` completo por ser copy-on-write de `production`. DP-01/02/03 y pendientes de demanda **no bloquean**. t-119/t-130/t-131/t-132 **resueltas** (2026-08-14). Quedan abiertas: t-128 (Neon/rollback — B-3 ya resuelto, falta B-4 plan de rollback), t-129 (dry-run ejecutado, B-2 no aprobado — 5 decisiones de negocio pendientes del Supervisor, ver `validacion_valores.md`).

**✅ BATCH DE DISEÑO t-136/t-137/t-138/t-139 — INPUT DIRECTO DE JAVIER, 4 DIAMANTES DELEGADOS A OPENCODE (2026-08-15).** Javier trajo 4 pedidos de diseño en una sola sesión (rediseño card P-01, dashboard de proyecto/timeline de gates, CRM nuevo, formulario P-27 a dos columnas). Se registraron como t-136..t-139 en el ledger y se delegaron a sub-agentes `opencode run` (rotación de modelos free por `MODELOS.md`), sin tocar código, produciendo/actualizando 4 `disenio_*.md`:
- **t-136** (`disenio_p01_kanban_comercial.md`, +v3): jerarquía de card (cliente→proyecto/tipo espacio→dirección, sin precio), badge mini con 5 tokens D4 propuestos (dot+pulse+glow), controles [→]/[←] in-card anclados a `parametros.transiciones_proyecto`. Coincide con "Lote C" que estaba diferido (línea de arriba, ya resuelto).
- **t-137** (`disenio_dashboard_proyecto.md`, nuevo, P-06): timeline horizontal con los 5 gates reales (E-18/E-21/E-24/E-33/E-20), bifurcaciones modeladas contra `reprocesos` (requiere campo nuevo `reprocesos.estado`, declarado pendiente de checkpoint), master-detail Espacios→Módulos→Ítems, overlays para Retoma/Calidad. Se pronuncia explícitamente sobre el hallazgo diferido D-16 (gates por proyecto vs. por nodo): v1 mantiene gates por proyecto, el salto a por-nodo queda declarado como riesgo alto de migración futura, no resuelto acá.
- **t-138** (`disenio_p28_crm_clientes.md`, nuevo): tablero + detalle de cliente sobre la entidad `clientes` ya existente, sin cambio de schema. Declara 4 hallazgos sin resolver (colisión de numeración P-27, `etapa_funnel` ausente de `schema.ts`, `citas`/`visitas` no materializadas, sidebar sin gating por rol).
- **t-139** ("Punto 3", que el mensaje original de Javier había omitido — lo agregó él mismo directo en `input_diseno_javier_20260815.md`): panel de dos columnas en P-27 + componente standalone `ProductoFicha` + campo `galeriaImagenesUrl`. **Hallazgo relevante:** al ejecutar el diamante se encontró que el working tree YA tiene esto implementado en código sin commitear (`app/erp/catalogo/page.tsx`, `components/veta/producto-ficha.tsx`, `galeriaImagenesUrl` en `lib/db/schema.ts` y `lib/data/contracts.ts`) — Javier lo construyó en paralelo mientras se delegaba el diseño. El diseño quedó como documentación retrospectiva; **la mutación de schema sigue marcada como pendiente de checkpoint explícito del Supervisor** (AGENTS.md lo exige aunque el código ya exista).
- Los 4 documentos quedan en estado **"propuesta — pendiente checkpoint Supervisor"**, no aprobados. Ningún archivo de código fue tocado por los sub-agentes (t-139 exceptuado por el hallazgo de arriba, que es trabajo de Javier, no de esta sesión).
- **Nota operativa:** 3 de los primeros intentos con modelos OpenRouter free (`google/gemma-4-31b-it`, `openai/gpt-oss-20b`, `nvidia/nemotron-3-super-120b-a12b`) fallaron — uno por rate-limit explícito, dos quedaron colgados sin output ni error (matados manualmente tras 6-13 min sin progreso). `opencode/big-pickle` resolvió las 4 tareas sin fallos.
- **Próximo paso:** Javier revisa los 4 `disenio_*.md` y da checkpoint explícito (incluida la mutación de schema de t-139) antes de que el Iniciador escriba planes de código.

**✅ IMPLEMENTACIÓN DE t-136/t-137(slice)/t-138 (2026-08-15, misma sesión que el batch de diseño).** Tras checkpoint verbal de Javier ("implementa" / "termina de implementar lo pendiente"), se pasó de diseño a código para 3 de las 4 tareas, delegado a sub-agentes `opencode run` (modelo `opencode/big-pickle`, tras fallos repetidos de varios modelos free de OpenRouter — ver nota de la entrada anterior):
- **t-136 (card P-01)**: `app/erp/comercial/page.tsx` + `app/globals.css` reescritos — jerarquía cliente→proyecto→dirección sin precio, badge-punto minimalista (5 tokens D4 nuevos `--badge-dot-*`/`--badge-label-*` + `animate-dot-mini`), controles [→]/[←] canónicos in-card. El menú ⋮ completo ("Cambiar estado →" con todos los destinos, Duplicar, Ver historial, Eliminar) sigue siendo un hueco preexistente de P-01, no tocado.
- **t-138 (CRM)**: `app/erp/clientes/page.tsx` + `app/erp/clientes/[clienteId]/page.tsx` nuevos + entrada "Clientes" en `erp-sidebar.tsx`. v1 sin columna "Etapa" ni pestaña "Visitas" (dependen de `clientes.etapa_funnel`/`citas`/`visitas`, no materializados en schema — H-CRM-2/H-CRM-3, sin resolver a propósito).
- **t-137 (dashboard proyecto), alcance recortado a "Slice 1+2" por decisión de Javier**: `app/erp/proyectos/[proyectoId]/page.tsx` reescrito — timeline de gates derivado (E-18/E-21/E-24/E-33/E-20/E-23/E-25/E-26, chip minimalista reusando el patrón de t-136, cero emojis), bifurcaciones de `reprocesos` como rama visual (sin abierta/cerrada, eso pide schema nuevo N1 fuera de alcance), árbol Espacios→Módulos→Ítem/planos, panel de dirección/instalación. La grilla "Ir a X" (Retoma/Esquema/Cronograma/Calidad/Instalación/Entrega/Documentos/Portafolio) se conservó igual. Los 4 overlays del diseño completo (`GateOverlay`/`OverlayRetoma`/`VeredictoModal`/`QuickActionModal`) y las 5 propuestas de schema (N1-N5) quedan explícitamente fuera — el diseño completo sigue disponible en `disenio_dashboard_proyecto.md` para una fase 2 si Javier la pide.
- **Verificación (las 3 tareas, re-verificada por el orquestador, no solo por el sub-agente):** `npx tsc --noEmit` 0 errores, `npx eslint` 0 errores en todos los archivos tocados. `next build` compila y pasa type-check/lint limpio pero choca con un `MODULE_NOT_FOUND` en `.next/server/pages/_document.js` (artefacto interno de Next, reproducido igual con `.next` borrado) — no atribuible a estos cambios, no verificado con `npm run dev` real por este agente.
- **t-139 CERRADA (2026-08-15, checkpoint verbal de Javier: "cierra ya esas tareas").** Verificación directa contra la DB real (`dev-local`, script de solo-lectura, no `drizzle-kit generate` interactivo): columna `galeria_imagenes_url` existe en `productos_catalogo` (`jsonb default '[]'`), y las 8 migraciones de `drizzle/v3/` (incluida `0006_add_galeria_catalogo`) están registradas en `drizzle.__drizzle_migrations` — sin drift, nada pendiente de aplicar. `disenio_p27_catalogo_diseno_desarrollo.md` actualizado a Estado: APROBADO.
- **Nada commiteado.** Pendiente: verificación visual de Javier vía `npm run dev` de t-136/t-137/t-138 (t-139 ya la vio y la confirmó).

**✅ F-09 CONSTRUIDAS — 6 LANDINGS DE ESPACIOS + FIX DE NAV/HOME ROTOS (2026-08-17, sesión agente, hallazgo de Javier).** Javier reportó que el menú "Espacios" mandaba a 404 y las cards del home mandaban directo a WhatsApp en vez de navegar. Diagnóstico: el dropdown de `app-shell.tsx` tenía 4 de 7 slugs que no coincidían ni con el diseño aprobado (`contenido_F09_landings.md`, Estado: aprobado 2026-08-09) ni con nada real; y de los 7 destinos, solo `pisos-de-madera` (F-14) tenía página construida — las 6 landings F-09 (contenido aprobado, código nunca escrito) faltaban por completo. Construido:
- **`components/veta/espacio-landing.tsx`** (nuevo): componente compartido Server Component, tokens D4 (no los `var(--color-primary)` más viejos de F-14) — hero con imagen real + CTA dual, descripción, Validación Técnica (3 cards) y Cómo trabajamos (4 pasos) reutilizados del home por diseño (`contenido_F09_landings.md` §3.8), CTA final, `BreadcrumbList` JSON-LD.
- **6 páginas nuevas** `app/(publico)/espacios/{cocinas-integrales-bogota,closets-vestidores-bogota,cavas-y-bares,consolas-recibidores,centros-de-entretenimiento,estudios-home-office}/page.tsx` — copy literal de `contenido_F09_landings.md` §3.2-3.7, sin texto nuevo. La primera (cocinas) construida directo para fijar el patrón; las otras 5 delegadas a un sub-agente Haiku (sustitución mecánica de datos, mismo patrón, ahorra contexto) y verificadas una por una tras el hallazgo.
- **Deliberadamente fuera de esta pasada** (mismo criterio que F-14): galería de 4-6 imágenes por categoría (solo hay 1 foto real por categoría en `public/images/home/`, verificado por listado de directorio) y las Respuestas Atómicas §4 del doc (marcadas "pendiente verificación con negocio real", una con placeholders `$X`/`$Y` sin resolver — no se publican precios inventados).
- **`components/veta/app-shell.tsx`:** los 4 slugs incorrectos del dropdown corregidos.
- **`app/(publico)/page.tsx`:** las 7 cards de "Espacios que creamos" ahora navegan (`Link`) a su landing real en vez de abrir WhatsApp; comentario obsoleto actualizado (F-09/F-10 ya existen, F-11/F-12/F-18 siguen sin construir y sus CTA siguen yendo a WhatsApp).
- **Verificado:** `tsc --noEmit` 0 errores, `eslint` 0 errores en los archivos tocados, `next build` 37/37 rutas — las 8 rutas de `/espacios/**` (índice + 6 landings + pisos-de-madera) prerenderizan estáticas (`○`).
- **Nada commiteado.** Pendiente: revisión visual de Javier vía `npm run dev`; recuperar imágenes reales adicionales por categoría (I-016) para poder construir la galería que hoy se omite; resolver las Respuestas Atómicas §4 con datos reales del negocio antes de publicarlas.

**✅ TIPADO ESPACIO→IMAGEN + RENDERS CONCEPTUALES HÍBRIDOS (2026-08-17, sesión agente, continuación directa del batch F-09).** Tras construir las 6 landings, Javier preguntó dónde se cargarían más fotos por categoría — la investigación (query de solo lectura contra `v3-preview`) encontró 122 proyectos/11 ya publicados en Portafolio con galerías reales de 1-24 fotos (~111 fotos sin usar en ninguna landing), y que `Portafolio.categoriaEspacio` (que el formulario ERP ya tipaba bien con 7 códigos correctos) tenía basura real (`"Residencial"`, `"residencial"`, `"Cocina y Zona Social"`) porque nunca se conectó con el tipo real del espacio del proyecto. Decisión de Javier: tipar todo (proyecto→espacio→imagen) de forma extensible y minimalista, sin pantalla de revisión masiva nueva — mejorar el formulario ya existente — más una estrategia híbrida (fotos reales + renders conceptuales propios, siempre etiquetados, nunca mezclados en silencio) mientras crece el stock real. Construido:
- **Catálogo único** `lib/catalogos/tipos-espacio.ts` (7 categorías, sin tabla de lookup — es una lista chica) — fuente única para el cotizador, el formulario de Portafolio, la pantalla de renders y las landings (antes repetido/desalineado en al menos 2 lugares).
- **Schema** (migración `drizzle/v3/0008_talented_magus.sql`, aditiva): `espacio_variantes.tipo_espacio` (nullable), `portafolio.espacio_variante_id` (FK nullable — la herencia real), tabla nueva `renders_conceptuales`. Aplicada contra la DB real y verificada (`drizzle-kit generate` → "No schema changes, nothing to migrate").
- **Hallazgo de infraestructura corregido de paso:** `drizzle.config.ts` tenía `out: './drizzle'` (carpeta legacy pre-V3) en vez de `'./drizzle/v3'` (la real) — causaba que `drizzle-kit generate` diffeara contra snapshots desactualizados y disparara prompts de ambigüedad falsos (ej. `contratos.especificaciones_desmonte`, ya resuelto hace semanas). Corregido el default; confirmado que ya no hace falta `DRIZZLE_OUT` a mano.
- **Capa de datos completa** (mismo patrón dual mock/drizzle de todo el repo): `contracts.ts`, `mock-store.ts`, `fixtures.ts`, `drizzle-impl.ts`, `snapshot.ts`, `actions/hydrate.ts`, `actions/core.ts`, `actions/portafolio.ts`, `actions/renders.ts` (nuevo, CRUD de renders), `actions/public.ts` (`obtenerGaleriaEspacioAction`: fotos reales publicadas de Portafolio por tipo; si una categoría no tiene ninguna, completa con renders conceptuales marcados `esRender:true`).
- **Cotizador** (`app/erp/cotizador/[proyectoId]/page.tsx`): selector de tipo al crear un espacio (2 call-sites duplicados unificados en un solo handler) + selector inline en el header de cada grupo de espacio ya existente (retroactivo, junto al lápiz de renombrar).
- **Formulario de Portafolio** (`app/erp/proyectos/[proyectoId]/portafolio/page.tsx`): nuevo selector "Espacio de origen" que hereda `categoriaEspacio` del `tipoEspacio` del espacio elegido (editable después); el `<select>` de categoría pasa a usar el catálogo compartido.
- **`/erp/portafolio`**: filtro de categoría usa el catálogo compartido; header con link a la pantalla nueva.
- **Pantalla nueva** `/erp/portafolio/renders` — cargar renders (tipo + imagen vía `ImagePicker`/R2 + título opcional) y gestionar visibilidad/borrado, agrupados por categoría.
- **Landings públicas**: `EspacioLanding` gana sección "Espacios que creamos" entre la descripción y Validación Técnica, con las fotos de Portafolio + renders juntas, sin distinción visible entre unas y otras (corrección 2026-08-17 misma sesión: la primera versión etiquetaba los renders "Render conceptual" y los ocultaba si ya había fotos reales — Javier pidió explícitamente que no, que es simplemente su galería libre por categoría). Las 6 landings F-09 + `pisos-de-madera` (F-14, que no usa el componente compartido) llaman `obtenerGaleriaEspacioAction` server-side y pasan `dynamic = 'force-dynamic'`.
- **Verificado:** `tsc --noEmit` 0 errores, `eslint .` 0 errores nuevos (mismos preexistentes de siempre, ninguno en archivos tocados). `next build` con `DATA_IMPL=drizzle` exit 0 — las 7 rutas de `/espacios/**` pasan a dinámicas (`ƒ`, antes `○` estáticas) al consultar datos reales; `/erp/portafolio/renders` compila.
- **Deliberadamente pendiente, es contenido no código:** los 11 registros de Portafolio ya publicados siguen con `categoriaEspacio` incorrecto — Javier los retipa uno por uno desde el formulario ya mejorado (selector "Espacio de origen" o directo la categoría), no requiere script ni pantalla adicional.
- **Nada commiteado.**

**✅ LOTES A + B + C — WEB PÚBLICA LISTA PARA LANZAR (2026-08-19, sesión agente).** Sesión de cierre de lanzamiento web: SEO/infra (Lote A), sistema de testimonios reales (Lote B) y documentación (Lote C). Lo relevante para el arnés:
- **Lote A (SEO/infra, sin schema ni lib/data):** sameAs real → `https://share.google/C4ERFWARygKWHkNGO` (checklist SEO #3/#4 cerrados); noindex `/cuenta` (`layout.tsx` con `robots`); JSON-LD BlogPosting/CreativeWork/BreadcrumbList en bitácora y portafolio; helper `lib/seo/social.ts` aplicado a 15 páginas (OG+Twitter); headers de seguridad + cache en `next.config.ts`; `app/manifest.ts` (apple-touch-icon.png pendiente — sin tooling, anotado en `TAREAS_DIFERIDAS.md` §8); `<img>` → `next/image` con `unoptimized` (URLs `r2.mock` no entran en `remotePatterns`; excepciones justificadas con eslint-disable).
- **Lote B (testimonios, migración aditiva + UI ERP):** columna `testimonios.nombre_autor` (text, nullable) — migración `drizzle/v3/0010_fresh_vision.sql` (aditiva; la 0009 ya existía: `personas.documento`); `publicados()` en DataStore (contracts/mock-store/drizzle-impl); `listarTestimoniosPublicadosAction()` en `lib/data/actions/public.ts`; pantalla ERP `/erp/portafolio/testimonios` + entrada en sidebar; Home data-driven (`listarTestimoniosPublicadosAction()`, JSON-LD desde datos, TESTIMONIOS hardcodeados eliminados); fixtures con `nombreAutor` real (Glenda Danuro, Daniela Barón Esparza, Juan Spiro, Madeline Attara — GBP 5/5); seed idempotente `scripts/seed-dev.ts` (UUID estable derivado del id de fixture; FKs a null porque los ids `mock-*` no existen en BD; allowlist ampliada con `ep-muddy-cherry-at5j2mz7` = host real de `.env.local` — ver discrepancia de línea 81). **4 testimonios sembrados y verificados** (`publicado=true`) contra la DB real.
- **Lote C (docs):** F-17 "Cotiza tu Espacio" oficialmente **POSTPUESTO post-lanzamiento** (`plan_diseno_web_publica.md` §0/§1/§2.5 + `TAREAS_DIFERIDAS.md` §7); **D-matriz de F-18 RESUELTA por el Supervisor (2026-08-19)** — copy final aplicado en `contenido_F18_conocenos.md` §9 (sin año 1971, sin "liderados por Víctor", abuelo fabricaba ladrillos, Hugo gestión de obras + infraestructura/plomería/electricidad/gas/acabados, Airhon centrado en "buen vivir" — interpretación asumida pendiente de confirmación de Javier; H1 "Tres generaciones construyendo. Un estudio diseñando." aprobado).
- **Verificación completa:** `tsc --noEmit` 0 · `eslint .` limpio en archivos tocados (errores restantes preexistentes en `scripts/*.ts` — no introducidos) · `next build` OK con `DATA_IMPL=mock` Y `DATA_IMPL=drizzle` · `drizzle-kit generate` sin drift · 76/76 tests mock-store.
- **⚠️ Impacto superado del plan B3:** las discrepancias listadas antes fueron cubiertas por el Bloque B3.

**✅ PLAN B3 COMPLETO — ARQUITECTURA WEB PÚBLICA FINALIZADA (2026-08-19, sesión agente).**
- **Fase 1 (D-Parámetro):** Creada la semilla `precio_asesoria_3d` en `fixtures.ts` y la Server Action `obtenerPrecioAsesoria3dAction`.
- **Fase 2 (Diseño):** 5 documentos canónicos de UI creados y aprobados (`F11`, `F12`, `F13`, `F18`, `F19`).
- **Fase 3 (Vistas React):** Codificadas las 5 rutas usando tokens D4 y degradación graciosa (F-13).
- **UX y Conversión:** Nav bar minimalista (se movió secundaria al footer); se eliminó el formulario estático B2B (F-19) por un embudo directo de WhatsApp de 1 día hábil; se inyectó "Navegación Contextual (Circuito cerrado)" en el Home (`F-01`) para atrapar leads orgánicamente.
- **Fix Home (Hallazgo):** Reemplazo de `<ImagenPlaceholder>` por `<Image>` optimizado en el carrusel de Portafolio del Home, leyendo `imagenPortafolioUrl` (resuelve bug visual).
- **SEO:** Rutas registradas en `sitemap.ts` y `llms.txt`.
- **Pendiente de reconciliación de Neon (heredado, ver línea 81):** `dev-local` (ep-little-dawn) podría estar detrás de `v3-preview` (ep-muddy-cherry) en migraciones; `.env.local` apunta a v3-preview. **Nada commiteado.**
 **Detalle completo:** `arnes/lineas/ola7/estado_ola7.md`.

**✅ LANZAMIENTO WEB PÚBLICA — SOLO "COCINAS INTEGRALES" (2026-08-25, sesión agente, checkpoint Javier).** Decisión de lanzamiento: el sitio público se publica con **una sola landing de espacio indexable y enlazada: Cocinas Integrales** (`/espacios/cocinas-integrales-bogota`). Cambios ejecutados:
- `components/veta/app-shell.tsx`: el tab "Espacios" (con su desplegable de 7 categorías) se reemplazó por el tab directo "Cocinas Integrales"; el footer "Colección de Espacios" quedó reducido a ese único link. Eliminado el array `categoriasEspacios`.
- Home (`app/(publico)/page.tsx`): la sección "Espacios que creamos" conserva las 7 cards como pieza comunicacional, pero ya **no enlazan** (`<div>`, no `<Link`).
- `noindex` (`robots: { index:false, follow:false }`) en las 6 landings restantes (`closets`, `centros`, `estudios`, `cavas`, `consolas`, `pisos-de-madera`) y en el hub `/espacios`. `sitemap.ts` y `llms.txt` realineados a solo cocinas (removidas las entradas `/espacios` y `/espacios/pisos-de-madera`).
- `arnes/TAREAS_DIFERIDAS.md` §9: registro de **pendientes de desbloqueo de landings** — cada una de las 6 requiere contenido real (textos, imágenes, descripciones) antes de quitársele el `noindex` y re-enlazarse. Criterio de desbloqueo documentado.
- **Verificado:** `tsc --noEmit` 0, `eslint .` 0 en archivos tocados, `next build` exit 0 (`DATA_IMPL=mock`). **Nada commiteado.**

**🚀 CORTE DE PRODUCCIÓN DEL SITIO PÚBLICO — DNS EN VIVO + PRIMERA MIGRACIÓN REAL A NEON `production` (2026-08-28, sesión agente, checkpoint Javier en vivo).** Javier reportó lentitud/UI rota en el ERP y pidió llegar al fondo; en paralelo decidió lanzar ya el sitio público aunque el ERP nuevo siga con bugs. Sesión larga, dos frentes:

**A) Bugs de fondo del ERP (no solo síntomas — causa raíz):**
- **Doble-submit en botones "crear" (27+ sitios).** `lib/hooks/usePendingGuard.ts` (nuevo): guarda síncrona por `useRef` + estado de UI, bloquea re-entrada mientras la acción está en vuelo. `components/veta/button.tsx` gana prop `loading` (spinner + disabled automático). Aplicado a los 27+ call-sites de `store.X.crear(...)` en `app/erp/**` (cotizador, compras, proyectos, equipo, finanzas, etc.) — ninguno tenía guard antes, confirmado por grep de `useTransition|isPending|useFormStatus` = 0 matches en todo `app/erp`.
- **Inputs de cantidad/precio "no reaccionaban" al escribir — causa real, no percepción.** `NumberInput`/`MoneyInput` (`components/veta/`) tenían `value` atado directo al store global sin buffer local; cada tecla disparaba un round-trip completo al servidor (`SELECT`+`UPDATE` en transacción) y cualquier re-render de por medio (frecuente, por el polling de reactividad) pisaba lo que el usuario acababa de escribir. Fix: `lib/hooks/useDebouncedInput.ts` — buffer local instantáneo + commit debounced (500ms/blur). Aplicado a `NumberInput`, `MoneyInput`, y al input suelto `grupoReferencial` en `cotizador/[proyectoId]/page.tsx`. Barrido confirmó que no hay más instancias del patrón en `app/erp/**`.
- **Piloto de "optimistic create"** en `proyectos.crear` (investigación previa: TanStack Query, Stripe idempotency keys, Linear sync engine, ensayo "Local-first" de Ink & Switch — plan en `C:\Users\javir\.claude\plans\dapper-marinating-river.md`). El id lo genera el cliente (`crypto.randomUUID()`) y es el id PERMANENTE desde el inicio (no hay reconciliación temporal→real, mismo patrón que Linear). `lib/data/actions/core.ts` (`crearProyectoAction`) usa `.onConflictDoNothing({ target: s.proyectos.id })` — idempotente ante reintentos/dobles-submits residuales. `lib/data/drizzle-impl.ts`: inserta optimista + `notify()` antes de esperar al servidor, revierte si falla; `applySnapshot()` gana un registro `pendientesOptimistas` para no perder la fila optimista si un snapshot llega a mitad de camino. `app/erp/cotizador/new/page.tsx` navega a `/erp/cotizador/{id}` sin esperar la confirmación. **Alcance deliberadamente acotado a este único flujo** — no se replicó a los otros 27 sitios de "crear" todavía.

**B) Corte de producción del sitio público (mismo proyecto Vercel, sin merge de git):**
- **No se creó un proyecto Vercel nuevo.** Se reutilizó `empresa-muebles-vl37` y se cambió su **Production Branch de `main` a `dev`** directamente en la config de Vercel — sin fusionar ni renombrar ramas de git (se descartó explícitamente esa opción: hubiera arriesgado el working tree principal de Javier, que sigue en `main`).
- **Producción de Vercel NO tenía `DATABASE_URL`/`DATA_IMPL`** (solo Preview) — el legacy nunca usó Postgres. Se agregaron al entorno Production apuntando a la rama `production` real de Neon (`br-twilight-mountain-atj7nwx6`, host `ep-round-queen-at3nzf87`).
- **Hallazgo crítico: la rama `production` de Neon estaba completamente vacía** — nunca se le habían aplicado migraciones Drizzle. Javier corrió `drizzle-kit migrate` manualmente (bloqueado para el agente por el clasificador de seguridad del entorno, acción correcta dado que escribe en la DB real) — 12 migraciones aplicadas limpio.
- **Hallazgo secundario, en el mismo pase:** la tabla `atributos_tecnicos` (agregada a `lib/db/schema.ts` por el commit `ed75124`, feature del slider "Validación Técnica") nunca tuvo migración generada — nadie corrió `drizzle-kit generate` después de tocar el schema. Causó `relation "atributos_tecnicos" does not exist` en producción real. Se escribió a mano `drizzle/v3/0012_atributos_tecnicos.sql` + entrada de journal (sin tocar el historial 0000-0011) y se aplicó — resuelto.
- **⚠️ Hallazgo sin resolver, pendiente de revisión con calma (no bloqueó el lanzamiento):** `drizzle-kit generate` falla con `[0010_snapshot.json, 0011_snapshot.json] are pointing to a parent snapshot: 0010_snapshot.json/snapshot.json which is a collision` — el historial de metadata de migraciones tiene un choque entre `0010` y `0011` (generadas aparentemente desde el mismo punto de partida sin que una rebasara sobre la otra). No impidió `migrate` (que no depende de los snapshots), pero **hay que arreglarlo antes de la próxima migración nueva** o `generate` va a seguir fallando.
- **Dominio conectado:** `vetadeoro.co` + `www.vetadeoro.co` (antes en Wix — 3 registros A + 1 CNAME a `cdn1.wixdns.net`) repuntados a Vercel (`A → 76.76.21.21` para ambos). Propagó rápido (minutos), SSL emitido, ambos responden HTTPS 200.
- **El ERP legacy queda intacto y accesible**, sin ningún cambio: `https://empresa-muebles-vl37-lte53e8dh.vercel.app` (deploy congelado de `main`, ~28 días de antigüedad al momento del corte). El equipo sigue cotizando ahí hasta que el ERP nuevo esté terminado — **es la URL de trabajo real hoy, no el dominio público**.
- **`AGENTS.md` actualizado** (sección "Modelo de repositorio y despliegue") para reflejar este modelo — el "Flujo de corte" que describía ahí (merge `dev`→`main` como único disparador de producción) queda histórico/superado para el sitio público; sigue vigente tal cual para cuando el ERP nuevo esté listo y haya que decidir qué pasa con `main`.
- **Pendiente, explícitamente fuera de esta sesión:** decidir qué hacer con `main`/`legacy-agnostic-backup` a mediano plazo (¿se abandona `main` como concepto de "producción futura" ahora que `dev` ya lo es?), resolver el choque de snapshots 0010/0011, y — cuando el ERP nuevo esté listo — decidir cómo se migran a la DB real los datos que el equipo siga cargando en el ERP legacy mientras tanto.

---

## Línea de demanda (captación, conversión, marca)

**Estado:** v3 del marco, sin aprobar el alcance de captación (Ads/GA4/Search Console, t-110 [SOLO_HUMANO]; informe de sector; schema del Bloque A, t-111; decisión de eslogan D1 — **resuelta 2026-08-09**, "Diseña tu espacio. Habita el bienestar."). Ese bloque de marketing sigue esperando al Supervisor. **En paralelo, la producción de la web pública ya avanzó sobre el contenido aprobado de esta línea** (ver Línea técnica): F-00 shell, F-09 landings, F-14 pisos, F-15 bitácora, SEO Lote A y testimonios Lote B implementados; **D-matriz de F-18 RESUELTA (2026-08-19)** y **F-17 POSTPUESTO post-lanzamiento**. El contenido de las 5 pantallas restantes (F-11/F-12/F-13/F-18/F-19) está aprobado y a la espera del plan B3.

**Próxima acción permitida (línea de demanda):** aprobar alcance de captación (`plan_demanda.md` §5) — no bloquea la web pública, que es el hilo activo de la línea técnica (plan B3 de F-11/F-12/F-13/F-18/F-19 pendiente de checkpoint del Supervisor). D2/D3/D5 ya no bloquean esas pantallas.

**Detalle completo:** `arnes/lineas/demanda/estado_demanda.md`.

**📋 PROPUESTA ABIERTA — PLAN MAESTRO DE PORTAFOLIO: FLOW DE USO (WIZARD) + SEPARACIÓN "CATEGORÍA DE LANDING" vs "TIPO DE ESPACIO ORGÁNICO" (2026-08-31).** Javier pidió el flow deseado del portafolio: crear entrada ¿asociada a proyecto? sí/no → crear espacios → añadir fotos (SEO auto) → publicar/despublicar/eliminar, independiente pero relacionable a `proyectos.id`. Al analizarlo surgió además la contradicción de los "huérfanos": la lista de tipos de espacio crecerá naturalmente para tipar la modulación de productos/espacios, pero no debe por eso forzar la creación de landings. Decisión: son conceptos distintos (categoría de landing fija vs tipo de espacio orgánico creciente). El plan `arnes/lineas/ola7/tecnico/plan_separacion_tipo_espacio.md` integra TODO en 3 fases: **F0** (t-146) wizard + `portafolio.proyecto_id` nullable para entradas libres; **F1** (t-147) materializar `catalogo_espacios_arquitectonicos` (taxonomía orgánica, diseñada en `OLA_6_SCHEMAS_APROBADOS.md:194-219` pero nunca construida); **F2** (t-148) desacoplar `tipoEspacio` (hoy sobrecargado) + migración auditada. **Pendiente de aprobación explícita del Supervisor (checkpoint obligatorio) antes de tocar `lib/db/schema.ts`** (mutaciones S1 cardinalidad, S2 tabla nueva, S3 semántica). También pendiente de Javier: decidir si registrar en el arnés o revertir las 2 desviaciones del trabajo anterior (botón eliminar en P-27 + mecanismo "categoría personalizada"; la F2 sustituye este último por la taxonomía real de todos modos). Antes de migrar: resolver el hallazgo de `drizzle-kit generate` (`[0010,0011] snapshot collision`, ver entrada del 2026-08-28).

---

## Artefactos canónicos del arnés (leer al arrancar)

1. `AGENTS.md` — zonas, prohibiciones, comandos
2. `estado.md` — este archivo
3. `INDEX.md` — índice de contexto activo
4. `MODELOS.md` — stack de modelos free
5. `nucleo/REGISTRO_DE_ENTIDADES.md` — schema canónico compartido (~60 entidades)
6. `nucleo/logica_de_negocio.md` — mapa de negocio compartido
7. `nucleo/glosario_h07.md` — vocabulario de UI compartido
 8. `ESTRUCTURA_OUTPUT_PRE_CODIGO.md` — gate de salida a código (línea técnica)
 9. `lineas/REGISTRO_LINEAS.md` — qué líneas de trabajo existen y qué producen
10. `TAREAS_DIFERIDAS.md` — tareas postergadas a propósito; **NINGUNA bloquea migración ni preview** (corta el bucle de fases que se reabren solas)

## Decisiones vigentes (aplican a todas las líneas)

- Stack: TypeScript en toda la pila. Next.js App Router. Drizzle ORM sobre la misma Neon.
- Sin motor schema-driven genérico.
- Tareas de riesgo alto o máximo pasan por checkpoint humano explícito antes de considerarse terminadas para producción real, aunque el commit en `dev` ya exista.
- Infraestructura de proveedores NO cambia.
- `main` no recibe push directo bajo ninguna circunstancia durante la migración.
- Ningún agente corre la app (`npm run dev`) ni prueba flujos de escritura mientras `DATABASE_URL` apunte a la Neon de producción compartida.
- **El código de las PoC del Diamante 4 (PoC 1/2/3/3.1, t-098/t-099) es prueba de concepto de estética/tokens/interacción únicamente.** Ninguna referencia puede citarlo como evidencia de que una pantalla de negocio "existe" o está aprobada — la única fuente de aprobación de pantalla es un `disenio_PXX.md`/`disenio_FXX.md` con checkpoint del Supervisor (decisión 2026-08-08).
- **Matryoshka por línea de trabajo (2026-08-08):** `nucleo/` es la verdad de negocio compartida; cada línea (`lineas/<nombre>/`) tiene su propio progreso, plan, y archivo histórico. Ver `lineas/_plantilla/LEEME.md` para abrir una línea nueva.

## ✅ FASE 0 ZUSTAND APROBADA — rama feature, sin tocar dev (2026-09-02, ZN-001)

**Registro de hallazgo/checkpoint (Acción 3 aprobada por Supervisor).** Tras dos rechazaciones del dictamen previo (10 errores de compilación en `useCotizadorStore.ts`), se re-hizo la Fase 0 de la línea de arquitectura zustand (`arnes/lineas/ola7/tecnico/zustand-migration/`):

- **Plan ZN-001** (`PLAN_ZN-001.md`, aprobado por Javier) — Fase 0 corregida: store Zustand 5 **solo lectura**, import **named** (`import { create } from 'zustand'`), fuente única de tipos (`CotizadorState` de `types.ts`), selectors reactivos con `useShallow`, **cero `any`**, sin optimistic (eso es Fase 2). Archivos de la rama:
  - `lib/data/stores/types.ts` (fuente única + `cotizadorInitialState()`)
  - `lib/data/stores/useCotizadorStore.ts` (estado `CotizadorState`, acciones `hidratar`/`avisarCambio`/`resetear`)
  - `lib/data/stores/selectors.ts` (`useSelectPorVariante`/`useSelectTotales`/`useSelectProductoMap`/`useSelectJornadasMap`/`useSelectGruposExpandidos`, tipados, sin `any`)
  - `lib/data/stores/useCotizadorStore.test.ts` (verificación ejecutable, patrón node:assert + tsx)
- **QA independiente: APROBADO.** Agente QA distinto del ejecutor verificó con evidencia mecánica: `npx tsc --noEmit` exit 0 (baseline previo 10 errores); test del store OK con placeholder de `DATABASE_URL`; `npx eslint lib/data/stores` 0 errores; `.env.local` real no tocado (`git status` limpio, `env.local.bak_pooler` eliminado); cotizador existente NO modificado (`git diff HEAD --stat -- "app/**"` vacío — Fase 0 solo crea archivos nuevos); cero `any`; import named confirmado; State usa `CotizadorState` como fuente única.
- **Limpieza:** eliminados todos los `tsc-*.txt` (basura del dev previo) y `env.local.bak_pooler` de la raíz. `.env.local` real intacto.
- **Commit:** `8b45988` `feat(arquitectura-zustand): fase 0 — stores zustand por dominio (solo lectura)` — **en la rama `feature/arquitectura-zustand-100-cotizadores`, NO en `dev` ni `main`.** La rama feature queda aislada: el cotizador real en `dev` no fue tocado (garantía de negocio).
- **Pendiente:** Fase 1 de ZU_03 (migrar `[proyectoId]/page.tsx` a los stores, selectors en UI) — fuera de este checkpoint; requiere su propio plan aprobado. `package.json`/`package-lock.json` llevan la dep `zustand@^5.0.15` (modificación del dev previo, requerida por el código nuevo).
