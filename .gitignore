# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
/dist/

# production
/build

# misc
.DS_Store
*.pem

# agno activity log — local only, never committed
.agno-log.jsonl

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ── PROJECT LAYER — never lives in the seed ──────────────────────────────────
# These directories belong to project forks. The seed has them empty (.gitkeep).
# Merging upstream/main into a project fork will NEVER touch these paths.

# Tenant storage — schemas, routes, data, scripts
storage/

# Project-specific custom blocks built with AI
# Exception: _TEMPLATE.tsx is part of the seed (shows the pattern to follow)
src/components/specialized/
!src/components/specialized/_TEMPLATE.tsx

# local Claude Code settings (machine-specific paths)
.claude/settings.local.json

.vercel
.env*.local
/.history/
